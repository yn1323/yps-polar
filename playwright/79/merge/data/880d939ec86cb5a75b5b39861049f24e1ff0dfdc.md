# Test info

- Name: サイドメニューナビゲーションテスト >> ログアウト機能が動作する
- Location: /home/runner/work/yps-polar/yps-polar/e2e/navigation/sideMenu.test.ts:74:3

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toHaveURL(expected)

Locator: locator(':root')
Expected pattern: /.*\/mypage.*/
Received string:  "http://localhost:3000/config/user"
Call log:
  - expect.toHaveURL with timeout 5000ms
  - waiting for locator(':root')
    6 × locator resolved to <html lang="ja" class="light">…</html>
      - unexpected value "http://localhost:3000/signin"
    3 × locator resolved to <html lang="ja" class="light">…</html>
      - unexpected value "http://localhost:3000/config/user"

    at /home/runner/work/yps-polar/yps-polar/e2e/navigation/sideMenu.test.ts:24:24
```

# Page snapshot

```yaml
- alert
- button "Open Next.js Dev Tools":
  - img
- region "bottom-end Notifications alt+T"
- paragraph: 管理画面
- link "マイページ":
  - /url: /mypage
  - button "マイページ":
    - img
    - paragraph: マイページ
- link "シフト":
  - /url: /shifts
  - button "シフト":
    - img
    - paragraph: シフト
- link "勤怠記録":
  - /url: /attendance
  - button "勤怠記録":
    - img
    - paragraph: 勤怠記録
- link "タイムカード":
  - /url: /timecard
  - button "タイムカード":
    - img
    - paragraph: タイムカード
- link "設定":
  - /url: /settings
  - button "設定":
    - img
    - paragraph: 設定
- button "ログアウト":
  - img
  - paragraph: ログアウト
- paragraph: ユーザー名登録
- group:
  - text: ユーザー名
  - textbox "ユーザー名"
- button "登録"
```

# Test source

```ts
   1 | import { login } from '@/e2e/utils/operations/auth/login';
   2 | import { registerUser } from '@/e2e/utils/operations/form/registerUser';
   3 | import { 
   4 |   navigationTestCases, 
   5 |   navigateViaSideMenu, 
   6 |   navigateDirectly, 
   7 |   verifyPageHeading, 
   8 |   verifySideMenuVisible 
   9 | } from '@/e2e/utils/operations/navigation/navigateToPage';
  10 | import { expect, test } from '@playwright/test';
  11 |
  12 | test.describe('サイドメニューナビゲーションテスト', () => {
  13 |   test.beforeEach(async ({ page }) => {
  14 |     // ログインしてマイページに移動
  15 |     await login(page);
  16 |     
  17 |     // ユーザー登録が必要な場合は実行
  18 |     if (page.url().includes('/config/user')) {
  19 |       await registerUser(page);
  20 |       await page.waitForURL('/mypage');
  21 |     }
  22 |     
  23 |     // マイページにいることを確認
> 24 |     await expect(page).toHaveURL(/.*\/mypage.*/);
     |                        ^ Error: Timed out 5000ms waiting for expect(locator).toHaveURL(expected)
  25 |   });
  26 |
  27 |   navigationTestCases.forEach(({ buttonText, url, expectedHeading }) => {
  28 |     test(`サイドメニュー経由で${buttonText}ページに遷移できる`, async ({ page }) => {
  29 |       // サイドメニューのボタンをクリックして遷移
  30 |       await navigateViaSideMenu(page, buttonText, url);
  31 |       
  32 |       // ページの見出しが正しく表示されることを確認
  33 |       await verifyPageHeading(page, expectedHeading);
  34 |     });
  35 |   });
  36 |
  37 |   test('直接URLアクセスで各ページに遷移できる', async ({ page }) => {
  38 |     for (const { url, expectedHeading } of navigationTestCases) {
  39 |       await navigateDirectly(page, url);
  40 |       await verifyPageHeading(page, expectedHeading);
  41 |     }
  42 |   });
  43 |
  44 |   test('サイドメニューのアクティブ状態が正しく表示される', async ({ page }) => {
  45 |     // 各ページに移動してアクティブボタンのスタイルを確認
  46 |     const activeTestCases = [
  47 |       { buttonText: 'シフト', url: '/shifts' },
  48 |       { buttonText: '勤怠記録', url: '/attendance' },
  49 |       { buttonText: 'タイムカード', url: '/timecard' },
  50 |       { buttonText: '設定', url: '/settings' },
  51 |       { buttonText: 'マイページ', url: '/mypage' }, // 最後にマイページに戻る
  52 |     ];
  53 |
  54 |     for (const { buttonText, url } of activeTestCases) {
  55 |       await navigateDirectly(page, url);
  56 |       
  57 |       // アクティブなボタンが表示されていることを確認
  58 |       const activeButton = page.getByRole('button', { name: buttonText });
  59 |       await expect(activeButton).toBeVisible();
  60 |       
  61 |       // ページのURLが正しいことも確認
  62 |       expect(page.url()).toContain(url);
  63 |     }
  64 |   });
  65 |
  66 |   test('サイドメニューが常に表示されている', async ({ page }) => {
  67 |     // 各ページでサイドメニューが表示されていることを確認
  68 |     for (const { url } of navigationTestCases) {
  69 |       await navigateDirectly(page, url);
  70 |       await verifySideMenuVisible(page);
  71 |     }
  72 |   });
  73 |
  74 |   test('ログアウト機能が動作する', async ({ page }) => {
  75 |     // サイドメニューからログアウトボタンをクリック
  76 |     await page.getByRole('button', { name: 'ログアウト' }).click();
  77 |     
  78 |     // ログアウト後は認証が必要なページにアクセスできないことを確認
  79 |     await page.goto('/mypage');
  80 |     await page.waitForURL('**/signin**');
  81 |     expect(page.url()).toContain('/signin');
  82 |   });
  83 | });
```

# Local changes

```diff
diff --git a/.github/workflows/playwright.yml b/.github/workflows/playwright.yml
index 797b836..253cf4d 100644
--- a/.github/workflows/playwright.yml
+++ b/.github/workflows/playwright.yml
@@ -48,3 +48,53 @@ jobs:
         name: playwright-report
         path: playwright-report/
         retention-days: 30
+
+  upload-to-github-pages:
+    needs: e2e
+    if: ${{ !cancelled() }}
+    runs-on: ubuntu-latest
+    permissions:
+      contents: write
+      pull-requests: write
+    timeout-minutes: 30
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+        with:
+          ref: gh-pages
+      - name: Download Playwright Report Artifact
+        uses: actions/download-artifact@v4
+        with:
+          name: playwright-report
+          path: "./playwright/${{ github.ref_name }}"
+      - name: Commit and push changes with retry
+        shell: bash
+        run: |
+          git config --global user.name 'github-actions[bot]'
+          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
+          git add playwright
+          git commit -m "chore: Update Playwright report in ${{ github.ref_name }}"
+          
+          # リトライロジック - 最大5回まで試行
+          for i in {1..5}; do
+            if git push; then
+              echo "Successfully pushed on attempt $i"
+              break
+            else
+              if [ $i -eq 5 ]; then
+                echo "Failed to push after 5 attempts"
+                exit 1
+              fi
+              echo "Push failed on attempt $i, retrying..."
+              sleep 5
+              git pull --rebase origin gh-pages
+            fi
+          done
+      - name: Create comments
+        if: github.event_name == 'pull_request'
+        uses: mshick/add-pr-comment@v2
+        with:
+          message-id: playwright
+          message: |
+            🎭 Playwright Test Report 🎭
+            [Playwright Report](https://yn1323.github.io/yps-polar/playwright/${{ github.ref_name }})
```